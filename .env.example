cat > ../../.env.example << 'EOF'
# ---------- FRONTEND ----------
# Copy this to apps/frontend/.env.local (do not commit)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ---------- BACKEND ----------
# Copy this to apps/backend/.env (do not commit)
PORT=4000
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
EOF
